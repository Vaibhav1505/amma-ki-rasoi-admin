// This layout is intentionally minimal.
// The root layout.js already handles rendering a bare shell
// when the user has no session (i.e., on the login page).
export default function LoginLayout({ children }) {
  return children;
}
